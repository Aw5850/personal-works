"use strict";

exports.prepend = function (a, b) {
  var c = a.concat();
  c.unshift(b);
  return c;
};

exports.append = function (a, b) {
  var c = a.concat();
  c.push(b);
  return c;
};

var foldPair = function foldPair(_x, _x2) {
  var _again = true;

  _function: while (_again) {
    var list = _x,
        result = _x2;
    _again = false;

    if (list.length > 0) {
      _x = list.slice(2);
      _x2 = result.concat([[list[0], list[1]]]);
      _again = true;
      continue _function;
    } else {
      return result;
    }
  }
};

exports.foldPair = function (list) {
  if (0 !== list.length % 2) {
    throw "object entried not paired";
  }

  return foldPair(list, []);
};